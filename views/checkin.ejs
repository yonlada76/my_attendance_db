<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Check-in List</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* ฟอนต์ที่ใช้ในหน้าเว็บ */
            background-color: #f0f8ff; /* สีพื้นหลังที่นุ่มนวล */
            margin: 0; /* ไม่มีระยะห่างรอบนอก */
            padding: 20px; /* ระยะห่างภายใน */
            color: #333; /* สีของตัวอักษร */
        }
        h1 {
            text-align: center; /* จัดกลางข้อความ */
            color: #0056b3; /* สีฟ้าสำหรับหัวข้อ */
            margin-bottom: 20px; /* ระยะห่างด้านล่าง */
            font-size: 2.5em; /* ขนาดตัวอักษรที่ใหญ่ขึ้น */
        }
        table {
            width: 100%; /* ขยายตารางให้เต็มความกว้าง */
            border-collapse: collapse; /* รวมเส้นขอบของเซลล์ */
            margin: 20px 0; /* ระยะห่างด้านบนและล่าง */
            background-color: #fff; /* สีพื้นหลังของตาราง */
            border-radius: 8px; /* มุมที่โค้งมน */
            overflow: hidden; /* ป้องกันการแสดงผลที่ไม่สวยงาม */
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); /* เงาให้ดูมีมิติ */
        }
        th, td {
            padding: 15px; /* ระยะห่างภายในเซลล์ */
            text-align: left; /* จัดข้อความไปทางซ้าย */
            border-bottom: 1px solid #ddd; /* เส้นขอบด้านล่าง */
            transition: background-color 0.3s; /* เอฟเฟกต์เมื่อเปลี่ยนสีพื้นหลัง */
        }
        th {
            background-color: #007bff; /* สีฟ้าสำหรับหัวตาราง */
            color: white; /* สีตัวอักษรในหัวตาราง */
            font-size: 1.2em; /* ขนาดตัวอักษรที่ใหญ่ขึ้นสำหรับหัวตาราง */
        }
        tr:hover {
            background-color: #f1f1f1; /* สีพื้นหลังที่เปลี่ยนเมื่อเมาส์อยู่เหนือแถว */
        }
        .status-present {
            color: green; /* สีเขียวสำหรับสถานะ "มา" */
            font-weight: bold; /* ทำให้ข้อความหนาขึ้น */
        }
        .status-absent {
            color: red; /* สีแดงสำหรับสถานะ "ไม่มา" */
            font-weight: bold; /* ทำให้ข้อความหนาขึ้น */
        }
    </style>
</head>
<body>
    <h1>Check-in List</h1>
    <table>
        <thead>
            <tr>
                <th>Student ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Active Date</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            <% studentLists.forEach(item => { %>
                <tr>
                    <td><%= item.student_id %></td>
                    <td><%= item.first_name %></td>
                    <td><%= item.last_name %></td>
                    <td><%= item.active_date.toISOString().split('T')[0] %></td>
                    <td class="<%= item.status === 'Y' ? 'status-present' : 'status-absent' %>">
                        <%= item.status === 'Y' ? 'Present' : 'Absent' %>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>
